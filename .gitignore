node_modules/
dist/
coverage/
*.tsbuildinfo

# Bun 独占依赖解析；bun.lock 要提交。一份游离的 pnpm-lock.yaml 会与它漂移
pnpm-lock.yaml

.env
.env.*
!.env.example

.DS_Store
*.log

# 教学工作区：讲义与学习记录含个人化内容，只留本地
learn/

# 测试夹具（tests/tools/mutating.test.ts 用完即删，这条防崩溃后残留）
.test-tmp/

# 量测用的可写工作副本（bench/measure-reread.ts 每趟重建）。不放 .scratch/ 下，
# 因为 Glob/Grep 看不见点目录，模型会找不到自己要改的文件。
bench-work/

# 规划工作区：wayfinder 地图与决策票，同 learn/ 一样只留本地
.scratch/

# 会话历史：开发时落在仓库内（NODE_ENV=production 时改落 ~/.mimicc/<repo>/）。
# 工具层也挡着它——见 src/tools/workspace.ts 的 SECRET，理由是 agent 不该读自己的过往对话。
.mimicc/
