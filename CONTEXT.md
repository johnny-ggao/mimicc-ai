# mimicc-ai

一个跑在终端里的编码 agent：调模型 → 有工具调用就执行 → 结果回灌 → 再调模型。这份文件只定词，
不讲实现——实现变了词不该跟着变。

## Language

### 上下文的来源与权限

**操作方通道（operator channel）**：
线上 `role: "system"` 的那条消息。**只有我们写的内容进这里**——它是唯一无法被对话中的其它参与方
伪造的通道，所以进了这里就等于拿到操作方权限。
_Avoid_: 系统消息、system prompt（指通道时）

**项目指令（project instructions）**：
仓库根的 `AGENTS.md` / `CLAUDE.md` 的内容。**权限低于系统提示词**——谁能提交这个仓库，谁就能写它。
名字里有 instructions，是因为我们确实要模型照做；但它不能覆盖系统提示词里的任何规则。
_Avoid_: 项目约定、仓库指令、项目上下文

**技能（skill）**：
装在工作目录之外（`~/.mimicc/skills`、`~/.claude/skills`）、**按需装载**的一组任务专用指令。
**权限低于系统提示词，也低于项目指令**：系统 > AGENTS.md > skill——仓库里写的规则要赢过一个装一次、
处处生效的通用流程。走 **user 通道**、钉住、带 `<skill name="…">` 标签，跟项目指令同一条 ADR 0001
判断，只是内容的作者从「能提交仓库的人」换成了「装这个 skill 的人」。装载是惰性的：常驻的只有一份
**技能目录（skill catalog）**——「模型可触发」skill 的名字 + 一句描述；正文靠两个入口之一按需拉进：
模型调 `Skill(name)` 工具，或用户在 REPL 敲 `/name`（斜杠命令）。frontmatter 里
`disable-model-invocation: true` 的 skill 只能斜杠触发——`Skill` 工具会拒绝它，因为那面旗子的全部
意义就是「人是索引」。**只挂主 agent**：目录注入和 `Skill` 工具加在主 agent 的中间件上，不进
`agentStack`，所以 Explore 子 agent 永远背不上它（同 0003：子 agent 能干什么 = 它的工具清单）。
_Avoid_: 插件、扩展（指技能时）

**注入（injection）**：
由 harness 而非模型，把内容放进消息历史。**与 prompt injection 是两个词**——后者是攻击，
中文里一律写全 `prompt injection`，不简称成「注入」。
_Avoid_: 装载、塞入、seed（指注入时）

**prompt injection**：
攻击面：进了上下文的内容里夹带指令，诱使模型把它当成操作方的话执行。**永远写全，不译**。

**附言（postscript）**：
harness 在**投影之后**追加到请求末尾的一条消息，**只活这一次调用**——不进对话历史、
不落 session 文件。今天用它送的都是**这一刻才成立的警告**（重复调用同一个工具、连续失败、
空回复），不是规则的复述。
⚠️ **和「注入」是两个词**：注入把内容放进历史，从此它属于这条 thread；附言不进历史，
所以它说过的话**在记录里查不到**——这既是它便宜的原因，也是它的代价。
⚠️ **这个名字只说位置和寿命，不说注意力。** 「排在尾部所以更受重视」在本仓库**没有证据支持**
（见「变化频率」），别把那个主张读进这个词里。
_Avoid_: 尾部高地（夹带了一个没有证据的注意力主张）、提醒、reminder

### 权限（工具执行）

**权限门（permission gate）**：
统一入口——一次工具调用先过它，得出 `allow` / `ask` / `deny` 三判之一。**它替换了此前散在两处的两个
机制**：确认门（只拦 `Bash`）和写死的路径约束（`resolveInside` + `SECRET`）。判定顺序 deny 恒胜
（deny > ask > allow），无命中落回内置基线（只读 allow / 改型 ask）。见 `docs/adr/0007`。
_Avoid_: 权限门限（与「确认门」念起来像一回事、实是两条轴）

**权限规则（permission rules）**：
权限门里用户可配的条目，三分桶 `allow` / `ask` / `deny`。文件工具按路径 glob（`Read(src/**)`），
`Bash` 按命令前缀（`Bash(git status:*)`）。两层：用户级 `~/.mimicc/permissions.json` + 仓库级
`.mimicc-permissions.json`，仓库只能更严。
_Avoid_: 白名单/黑名单（那是没有 ask 的两分）

**确认门（confirmation gate）**：
权限门三判里的「ask」那一条——停下问人。**它不再是独立机制，而是权限门的一个出口**；拒绝理由仍
钉住（见「钉住」）。
_Avoid_: 权限门（那是三判的总称）

**自动模式（auto mode）**：
会话级姿态开关，把默认 `ask` 翻成 `allow`。**只动「问不问」轴，不动 deny / 硬地板**——与「绕过」
是两回事，绕过会吃掉硬地板、明确不做。**显式开关**：要敲 `--auto`（`src/console/args.ts:26`），
不是默认姿态。
🔑 **它只翻基线，不翻规则**：`decide` 的规则循环跑在 `auto` 那一行**之前**
（`src/tools/permission.ts:168-185`），所以**一条显式的 `ask` 规则在 `auto` 下仍然是 ask，
`deny` 仍然是 deny**（2026-08-25 实测）。**这一条决定了「auto 下某类操作还想要一道关」
该怎么表达**：不是给 auto 加例外，是配一条规则——**`auto` 是用户说的「别问」，
规则是他说的「这几条还是要问」，两句话都该由他说。**
_Avoid_: 给 auto 开例外（那会把「只动一根轴」这句话弄浑）
_Avoid_: YOLO 模式、绕过（绕过是另一件事）

**硬地板（hard floor）**：
权限门里不可放宽的那层 deny：逃出工作目录 + 密钥路径（`.env`、`.git/`、`.mimicc/`、`id_*`、
`*.pem`、`*.key`）。任何层的规则都不能把它翻成 allow/ask。
⚠️ **「逃出工作目录」这一条对 `Read` 不成立**（2026-08-28 判，ADR 0007 有订正）：
`Read` 出得去，`Write`/`Edit` 出不去。理由是这道门对读**从来没挡住过**——
Terminal-Bench 四次拒绝，四次模型改用 `Bash` 的 `cat`/heredoc 拿到了同样的东西，
净效果只是多烧一圈并把动作赶到逃生口上。放行之后凭据地板补了仓库外的那一半
（`SECRET_OUTSIDE`：`~/.ssh`、`~/.aws`、`~/.docker`、`~/.config/gcloud`、`.netrc` 等）。
🔴 **它按路径判，所以够不到 `Bash`**（`src/tools/permission.ts:212-215` 逐字：
_the hard floor does not reach `Bash`, so `cat .env` would leak the secret's value_
——这正是读内容的命令被刻意排除在安全清单之外的理由）。见「逃生口」。

**逃生口（escape hatch）**：
`Bash`。**两条按路径判的防线都够不到它**——硬地板够不到（见上），版本闸也够不到
（它按工具名判，而认出一条命令写不写文件要解析 shell，那不是二值判据）。
**把关它的是确认门**：安全清单只有 `ls` / `pwd` / `git status` / `git branch` 四条免问
（`src/tools/permission.ts:216`），其余一律 ask。
🔴 **所以 `auto` 下它没有默认的关**。要更严就配规则（见「自动模式」）——
⚠️ **但规则本身也只到前缀**：`Bash(printf:*)` 拦得住 `printf … > f`，拦不住
`cd src && printf … > f`（2026-08-26 实测）。**「写文件的命令」这个条件，今天的规则说不出来。**
⚠️ **这是边界，不是缺口**：这个仓库对 shell 的推理深度**只到前缀，不到语义**
（`permission.ts:123`），在别处重新发明它会让同一个问题有两个不一致的答案。

**版本闸（version gate）**：
改一个**已存在**的文件，要求这次对话里有它**当前版本**的一次 `Read`——或**你自己的 `Write`**，
它盖同款标记（自己写下的字节就是当前版本，2026-08-27 增补）——`src/agents/readBeforeWrite.ts`。
**只管 `Edit`**：`Write` 本来就永不覆盖（`src/tools/mutating.ts:48-54`），
**一条已经被更严的规则覆盖的路不该再被一条更松的规则覆盖**，第二条只会让第一条更难读。
判据是二值的：**文件存在 且 没有当前版本的读/写标记**。哈希不出来时**放行**——
**闸的失效不该比没有闸更糟**。见 `docs/adr/0008`（含 Write 盖章的增补）。
⚠️ **与权限门是两条轴**：权限门判「许不许」，版本闸判「你是不是拿着旧的在改」。装在权限门内侧，
所以硬地板拒绝的路径不会被它打开读一遍。
**它有一半是眼睛**：`src/agents/staleReads.ts` 在每条 `Bash` 前后各给带标记的文件做一次快照，
变了就发一条**一跳就消失**的提醒——那条路上没有工具调用可拦，只能告知。

**读标记（read mark）**：
一次 `Read` 或一次成功的 `Write` / `Edit` 在它的 `ToolMessage` 上留下的
「哪个文件 + 当时的 sha256」。
**盖在 `additional_kwargs` 上，与钉住标记同一个位置**——所以**摘要吃掉那条结果，标记跟着没，
闸就再也过不去**。🔑 **这不是巧合**：投影决定模型看得见什么，闸的状态就该活在模型看得见的那份里。
**写完就盖新章**（`Write` 2026-08-27、`Edit` 2026-08-28）：工具自己算得出写完的字节，
所以「知道当前版本」是事实不是客气。**作废的是此前的读**，不是新盖的这一枚——
两次连续修改之间**不必**重读。**外部改动**（Bash、用户）让哈希对不上，标记照样作废、闸照样拦。
_Avoid_: 缓存、快照（那是 `staleReads` 在命令前后各取的那两份）

### 上下文的成本

**缓存前缀（cache prefix）**：
provider 按最长公共前缀命中的那一段。**请求里任何逐次变化的名字或编号都会砸掉它**，
而层序（哪一段排在前面）由 provider 决定，不由我们决定。
_Avoid_: 前缀缓存、KV cache

**变化频率（change frequency）**：
一段内容两次请求之间变不变。**它决定这段该排在哪**——不由重要性决定。
**方向是全序的：最不常变的排最前，最常变的排最尾。** 因为一段内容一变，**它之后**的整片缓存
前缀就作废——变得越勤，身后越不该压着东西（`repro/31-the-cache-bill.ts`：同一段内容逐位相同、
只差位置，放头部命中 4.0%，放尾部 39.3%）。
⚠️ **判据是「变不变」，不是「重不重新组装」。** 每轮重算一遍、算出来却逐位相同的那种
（记忆就是），排在最前面是对的——把判据挂在「重不重新组装」上，是拿机制当性质。
⚠️ **「不由重要性决定」是量过的，不是省事。** 同一条规则放三个位置，31.7 万 token 深处的
遵循与取回没有可测差异（`repro/37-does-position-change-adherence.ts`）——位置这根轴上
没有「重要的往显眼处放」这回事。⚠️ 那是满分档，只能说**没测出效应**，不是证明效应不存在。

**所有权（ownership）**：
谁能写这段内容。**它决定这段该进哪条通道**——与变化频率是两条独立的轴。

**上下文窗口（context window）**：
模型这一次实际看见的内容——从全部可用上下文里**筛选、压缩、排序之后**得到的那一份。
**它是算出来的视图，不是一段被剪短的历史**；原件是对话历史，两者是两样东西。
算它的那个动作叫**投影**（见下）。
⚠️ 与 langchain 文档相反：它把「短期记忆」定义在 checkpointer 那一端，也就是我们说的对话历史。
_Avoid_: 短期记忆（除非明确说的是 langchain 的用法）

**投影（projection）**：
从对话历史算出上下文窗口的那个动作——`src/context/projection.ts`。**「视图」是名词（那份
`BaseMessage[]`），「投影」是造它的动词**：两个输入（历史、切点）、一个输出、
**没有副作用、不碰框架**。哪些消息必须活下来不是第三个输入，它写在消息自己身上（见「钉住」）。
它每次调模型重算一遍，落盘的只有两个数（切点 + 摘要那一条消息）。
**「压缩上下文不等于删历史」这句话之所以成立，全靠这里是投影而不是原地改写。**

⚠️ **它必须是确定的**：同一段历史两次投影必须得到同一份视图，否则「投影」这个词就名不副实，
而这个仓库每一条关于缓存前缀的论证都建立在这上面。**这条确定性有一个外部依赖：
并行工具的结果必须按源序进历史**，而那是 langchain 默认 `messages` 通道的性质、不是我们的
——换掉那个通道就会丢（`tests/source-order.test.ts` 钉着这条）。
_Avoid_: 裁剪、截断、compact（指这个动作时）

**钉住（pinned）**：
一条消息**不许被摘要吃掉**——切点走过它，投影也要把它捞回来。
**判据是二值的，写成一个函数就是「带没带钉住标记」**（`additional_kwargs` 上一个键）。
⚠️ **不是「重要」，是「带没带标记」**——重要性是打标记那一刻的判断，投影不重新判一次。

被钉住的东西按**谁来打**分三类（2026-08-25 重核，原文只列了三样且漏了技能正文）：

- **注入者在构造时打**（`PINNED` 直接铺进 `additional_kwargs`）：**项目指令**、
  **技能目录**、**注入的记忆**。
- **图在 `beforeAgent` 打**：**这一轮的原始任务**（不是控制台打——挂在调用方身上的保证不算保证）。
- ⚠️ **例外补标**（`markPinned`，共三处）：这些 `ToolMessage` 由 langchain 生成，
  我们没有构造函数可以伸手。**每多用一次都要说明为什么规则在那里不适用**：
  1. **确认门的拒绝理由**（`src/agents/loop.ts:529`）——`markPinned` 就是为它存在的；
  2. **技能正文**（`src/skills/tool.ts:124`）——一段被摘要吃掉一半的指令，是模型会停止遵守的指令；
  3. **版本闸的拒绝**（`src/agents/readBeforeWrite.ts:219`）——理由与第 1 条同类：
     **一条被摘要吃掉的拒绝，等于让模型重试那次陈旧的写**。
     ⚠️ 对照：`staleReads` 的提醒**不钉住也不进历史**，因为它的作用期只有一跳——
     **产物的存续期要匹配它的作用期**，这是同一条规则的两个方向。

⚠️ **钉住不是「值钱」的同义词。** 判据是**「丢了它模型会做错事」**：拒绝理由丢了模型会重试你
刚拒绝的命令，原始任务丢了它不知道在干什么。子 agent 的报告很贵，但丢了它模型只会重新派一次
——所以它不钉住。
_Avoid_: 保留、锁定、pin（中文里）、重要消息

**降级（downgrade）**：
把一条消息**换成一个更小的等价物**——既不逐字保留，也不整个交给摘要。**第三条路。**
⚠️ **判据不挂在消息上，挂在内容类型和体积上**：一条工具结果，超过阈值。
所以它和「钉住」是两个正交机制，不是一根刻度的两端——把「一份 60KB 的文件清单」和
「用户的拒绝理由」排在同一根刻度上，得到的是一根没有意义的刻度。

三档，从轻到重：**逐字**（不动）／**提要 + 指针**（一份结构化提要，加一个能取回全文的路径）／
**截断**（写不了盘时的兜底，首尾各留一段）。

两条硬规矩：

- **提要由确定性代码生成，不叫模型。** 一次降级不该再花一次模型钱；更要紧的是**同样的输入
  必须产出同样的提要**，否则同一段历史在两次运行里投影出两份不同的视图——而那正是
  「投影」这个词要求的性质。
- **降级产物要主动交代自己是什么。** 一份读起来像原文的提要，模型会当原文用。

⚠️ **这一跳的工具结果永不降级。** 那既是「模型正要用它」，也是唯一那条循环的闸：
指针只有在 `Read` 打得开时才有用，而如果取回来的全文一到就被降级，模型拿到的永远是
它刚去取的那个东西的提要。判据是**位置**不是工具名——所以它对还没写出来的工具也成立。
_Avoid_: 压缩（那是摘要）、截断（那只是第三档）、精简

**常驻段（resident）**：
每次请求都在、且与这条 thread 无关的那一段：**系统提示词 + 工具定义**。
**一次请求 = 常驻段 + 视图**——这条等式是必须的，因为触发摘要用的数（provider 报的
`input_tokens`）**含常驻段**，而挑切点用的数（消息数组的估计）**不含**。两者不是两把尺不准，
是量的根本是两样东西。生产上它约 2400 token、占窗口 0.2%，看不出来；探针尺度上它是大头。
_Avoid_: 固定开销、前缀（那是缓存前缀）、系统段

**冻结块（frozen block）**：
一条**造一次、此后正文逐位不变**的注入消息，且**钉住**——所以它活过每一次切点。
判据是「正文变不变」，**不是「每轮重不重新组装」**：每轮重算一遍、算出来逐位相同的，也是冻结块。
**常驻段 + 冻结块 = 缓存前缀能靠得住的那一段**：前者在消息数组之外，后者在里面。
⚠️ **一个块只要会变，它就不在这一类里**——而它一变，**它之后的整片前缀作废**（见「变化频率」）。
_Avoid_: 快照（通用概念，且它说的是「某一刻的副本」，而这一类的要害是**此后不再变**）、
静态块、常驻段（那是数组之外的）

**对话历史（conversation history）**：
这条 thread 上发生过的全部消息，**永不丢弃**，落在 checkpointer 里。上下文窗口是它的视图，
所以压缩上下文不等于删历史——**能不能压得起，取决于原件是不是真的还在**。
_Avoid_: 消息列表、transcript（中文里）

**窗口上限（window limit）**：
一次请求装得下多少 token。`deepseek-v4-flash` 与 `glm-5.3-flash` 都是 **1,048,576**
（两个都是实测，撞上去是硬 400）。
**它约束的是单次请求，不是一条 session 的总和**——六次请求各花 3000，不等于用掉了 18000。
单次请求 = 常驻段（系统提示词 + 工具定义）+ 这条 thread 至今的全部历史
**（在有些 provider 上还要加上这次为回复预留的额度 `max_tokens`）**，所以
**thread 越长、单次请求越大**，这才是窗口会满的机制。
🔑 **预留的输出额度算不算进窗口，是 provider 的选择，不是窗口的定义**——这一句到
2026-08-30 之前一直写成后者，而第三家把它推翻了（`repro/33`，每家都带对照组）：

- DeepSeek **算**。逐字：_maximum context length is 1048576 tokens. However, you
  requested 1249764 tokens (856548 in the messages, 393216 in the completion)_。
  所以在它上面**要多少输出，就少多少历史**。
- 智谱 **不算**。971,327 的输入配 `max_tokens: 131,072`，两者之和 1,102,399 已经超过
  同样是 1,048,576 的窗口，**照样 200**。
  ⚠️ **所以这条约束要按最严的那家写**：发到线上的输出额度必须远小于压缩阈值留下的余量——
  在不算的那家上这只是浪费一点余量，在算的那家上这是溢出保护来不来得及触发的分界。
  **这个数只能从 provider 的文档或实测拿**：API 不返回它，SDK 会替你编一个。
  ⚠️ **文档给的「1M」不是这个数**：智谱所有页面只写 1M，而 1,000,000 与 1,048,576 差 48,576。
  注册表一度取了低读法（在溢出码认不出来的 provider 上，读高了是请求直接失败），
  `repro/55-is-the-registered-window-the-real-one.ts` 证明那是错的。**保守的猜测仍然是猜测。**
  _Avoid_: 上下文长度、context size、context window（指上限时）

**溢出保护（overflow protection）**：
为了不撞上窗口而做的裁剪，与**成本优化**是两件事，判据相反——**成本优化按期望值算，划不来就
不做；溢出保护按最坏情况算，一年触发一次也要正确**。把两者混在一个阈值里，会得到一个既不省钱
又守不住的数。
_Avoid_: 上下文压缩、省 token（指这类机制的目的时）

**溢出码（overflow code）**：
provider 用来说「这次的 prompt 超窗了」的那个业务码。**它是溢出保护的触发条件**——
保护本身不认错误，它只问框架有没有把这次失败判成溢出，而框架靠匹配几句写死的英文来判。
🔴 **「恰好命中」不是机制。** DeepSeek 的拒绝里有 `maximum context length`，正好是那几句
之一——那是核过的，不是设计出来的；智谱回的是 `400 code=1261 "Prompt exceeds max length"`，
一句不沾，于是**一次教科书式的超窗被判成普通失败，摘要一次都不会跑**
（`repro/53-does-the-overflow-reach-us.ts`）。**认不出溢出的溢出保护，不是弱一点，是没有。**
🔑 **认码，不认文案。** 同一个 1261，错误码页印的是「Prompt 超长」，线上发的是英文——
文案在文档和线之间就已经漂了,而码是那一页真正承诺的东西。
⚠️ **它是 provider 知识，所以住在注册表**（`ProviderSpec.overflowCodes`），和窗口上限一起
递进 `contextWindow`；`src/context/compaction.ts` 到今天仍然不认识任何一家 provider，
这条边界是故意的。⚠️ **一家的码是空的，不等于没查**——等于查过、框架自己认得。
_Avoid_: 错误码（太泛，指的是整张表）、超长报错（那是文案，不是判据）

**思考过程（reasoning）**：
模型在给出回复之前产生的那段推理，provider 放在 `reasoning_content` 里，与**回复正文**
同一条流、不同字段。**它不是回复的草稿，也不是可以丢的边角料**——两条硬约束：
①**它必须原样回传**（Moonshot 的 `kimi-k3` / `kimi-k2.7-code` 无条件要求，`kimi-k2.6`
在工具循环里丢了会报错），所以它和正文一样吃窗口；②**关掉它不省钱**（实测总 token 反而多七成）。
**每一次模型调用产生一段**，所以一个回合里的段数 = 1 + 工具跳数。
_Avoid_: 思维链的草稿、模型的内心戏、debug 输出

**痕迹（trace）**：
思考过程在**转录**上留下的那一行（`· 思考 356 字`），不是思考过程本身。
两者的边界是这条线的全部：**原文永远全量落盘，折叠只发生在渲染那一层**——
任何在持久化之前就丢信息的实现，都会让「事后翻回来看」变成一句空话。
🔑 **痕迹的内容必须从存下来的消息算得出来**，否则「活着的那份」与「恢复的那份」
印不出同一行——而那两份必须长得一样，理由与工具调用行相同：
**一份走到一半就换形状的转录，比两种形状里的任何一种都糟。**
_Avoid_: 摘要（那是降级产物）、折叠后的思考过程

### 循环

**回合（turn / run）**：
一次 `graph.stream()` 调用，从调用到它返回。**一个回合由恰好一条用户消息开启**——不管那条
消息是当场敲的，还是从排队的输入里取的。一个回合里模型可能被调用多次。
⚠️ **不要用「用户按一次回车到交还提示符之间」定义它**（2026-08-17 之前就是这么写的）：
那从系统内部不可判定，而且不准——实测（`repro/15-typing-during-a-turn.ts`）回合进行中敲的
行会被缓冲、回合结束后补放，**开启下一个回合的那次回车，是在上一个回合里按的**。
_Avoid_: session（一条 session 是一串回合）、轮次（指一次模型调用时）

**排队的输入（nextRun）**：
回合进行中敲下的那一行。它**不进当前回合**，排在后面自成一个新回合，而且开跑之前会
**说一声它是排队来的**——不然用户无法把它和自己刚才那句区分开。名字取自 pi 的
`nextRun` 队列（`harness.md:1431`：任何状态都能进、从不启动 run）。
⚠️ **一处不同**：pi 的 `nextRun` 逐字 _never starts a run_，我们的主循环取到就开跑——
在 REPL 里没区别（**没有第二个启动者**），换成库就有。
**至多一条。** 排到第二条意味着用户在对一个自己还没看见输出的回合提前下指令；
多出来的**不入队**，出声丢掉。⚠️ 这条只在终端上成立：管道里整段脚本在进程启动前就写好，
它的顺序是刻意的，没有哪一次敲键需要被保护（与「哪一行属于谁」是同一条边界）。
**只有中止能清空它**，因为中止是队列**之外**的正交控制信号；`/clear` `/resume` 这些**自己就是
队列里的一条行**，排在它们前面的是用户先敲的，先来后到就是诚实的顺序。
⚠️ **它的行为源自 readline 的缓冲**（`repro/15` 实测），但**队列本身是控制台自己的**：
行在敲下的那一刻入队并记下它在回答谁。**这个区别决定了上面三句能不能成立**——
借来的缓冲管不了，自己的队列才管得了。
_Avoid_: steer（那是插进当前回合、改这一跳方向的，我们没有）、follow-up（同样没有，且明确不做）

**跳（lap）**：
回合内的一次「调模型 → 跑工具」。`beforeAgent` 每回合跑一次，`wrapModelCall` 每跳跑一次。
⚠️ **预算不按跳计**——没有步数/圈数上限（2026-08-27 删），预算在 token 与墙钟两个轴上（见「回合预算」）。

**中止（abort）**：
回合进行中按 Ctrl+C 触发的那个控制信号。**它是正交控制，不是失败也不是崩溃**——状态机之外、请求当前回合停下来的信号（pi `harness.md` §4.6 逐字 _Abort is not a phase. It is `control`._）。
⚠️ 与「失败」的边界：中止是**人**要停，失败是**事**没成（500 / 超限）。中止不重试、不救活；失败可重试、可救活。
⚠️ 与「崩溃」的边界：中止是**有序的**——run 知道自己被中止、回合照常收口；崩溃是**无序的**——进程被 kill、只能靠检查点恢复。
_Avoid_: 取消、打断（当作异常语义时）

**失败（failure）**：
一个回合没跑成（模型 500、请求超限、重试耗尽）。**它是可救活的状态，不是终点**——新输入能救活它（pi `harness.md` §3.12 `failure_drain`）。与「中止」的边界见上。
⚠️ **失败在历史里留下自己的记录，中止不留。** 一个失败的回合会补上一条 harness 写的
标记消息，下一个回合读得到它——所以那条用户消息**是被回答了的，只是回答它的不是模型**
（`repro/24` 实测形状 `human → ai`）。这是这两个词最容易被搞混的一处边界：
两者都让回合停下，但只有失败在盘上留痕。
_Avoid_: 错误、异常（把失败当终点时）

**被 cap 的完成（capped）**：
回合被机制逼停、但已逼出最终答案的收尾——**第三态，既不是失败也不是干净成功**（票 17 / ADR 0005）。
机制有两个：loop guard 的重复调用（`loop_capped`），与回合预算耗尽（`budget_exhausted`）。
🔑 **它不是从 error 分类出来的**：guard 在回合里 push `onCap(...)`、不抛异常（票 01），所以与「中止」「失败」不是一条轴。
⚠️ 与「失败」的边界：失败是**事没成**（500 / 超限 / 重试耗尽），写失败 marker、下一轮救活；被 cap 是**事被迫收了个尾**（有最终答案），不写 marker，只记结构化 `turn_capped`。
⚠️ 与「中止」的边界：中止是**人**要停，被 cap 是**机制**逼停，回合都照常收口。
_Avoid_: 失败（capped 不是失败）、中止

**回合预算（turn budget）**：
一个回合在 **token 与墙钟**两个轴上能花多少，花超了还收不了尾就被 cap。
🔑 **没有步数上限**——步数（跳/节点）不作为预算（2026-08-27 删，ADR 0009）。三家 peer 都不按
步数计（pi `while(true)`、DSH 直到模型完成或 max-tokens、deer-flow 单一崩溃网），按步数计会把
「健康地忙」误判成「坏了」——csv-to-parquet 三连撞顶（48 → 102 → 150 节点）就是证据。
默认：**token = 窗口 × 4**（每次调用的 input ≈ 当前视图大小，预算随窗口自标定，不逐任务拍数）、
**墙钟 = 10 分钟**；`MIMICC_TURN_TOKEN_BUDGET_MULTIPLIER` / `MIMICC_TURN_TIME_BUDGET_MS` 可配。
🔑 **耗尽不是失败**：注入一条说明，给模型一次**不带新工具调用**的交卷机会；它还发工具调用才被
cap（`budget_exhausted` + 罐头收尾）。`RECURSION_LIMIT` 因此退成 langgraph 的格式占位
（它要求 ≥1 的有限数，取 1_000_000），真正的护栏 = 回合预算 + loop guard + stall guard + 墙钟。
🔴 **那个「墙钟」不是钟，是预算的第二个轴**（ADR 0010）：判据写在 `afterModel` 里，**查表，
不是定时器**——查表要等模型这一跳先返回，而那一跳自己没有上界（`repro/50` 实测：把它压到 5 秒，
一个挂住的流照样让回合无限期挂着，它一次都没醒）。所以它的真实含义是**「不早于 10 分钟」**：
过期之后还要再走一轮工具 + 一次模型调用才收得了尾。**要一个真会响的钟，见「期限」。**
_Avoid_: 步数预算、圈数、lap budget、期限（预算是「花多少」，期限是「到什么时候」）

**期限（deadline）**：
一个**绝对时刻**，过了它这个进程不该还在干活。**每一层的期限由外层的剩余时间夹取**
（`inner = min(它自己想要的, 外层剩余 − 余量)`），而最外层必须是一个**真会响**的钟——
到点有人被叫醒，不是下一次路过时才发现过期了（ADR 0010）。
🔑 **只在没人挂着的时候存在。** `--print` 从入口拿一个进程级期限（`--timeout <秒>`，没给就退到
**回合墙钟加上收尾余地**）；交互式没有总闸，**因为人就是那把钟**——他盯着命令跑着的那一行按
Ctrl+C。这与单条命令的默认期限用的是同一条判据，不是新增的一条轴。
⚠️ **回合预算反过来被它夹取**（「剩余 − 收尾余地」），因为预算同样是内层的钟：不夹的话，
一次 `--timeout 340` 的调用里那个十分钟的预算永远轮不到响，而**轮不到响的预算意味着交不出
答案**。默认路径上这两项抵消，回合预算还是配置的那个值。
⚠️ **是时刻，不是时长。** 夹取要回答「还剩多少」，而时长在层与层之间传递会被每一层重新起算——
那正是「单次模型请求 600 秒」与「回合墙钟 600 秒」互相看不见的机制。
⚠️ **与「中止」的边界**：中止是人要停、不写 marker；期限到了是**机制**停手，**写 failure marker
并报出是哪只钟、过了多久**（`classify` 的 `deadline`）。
⚠️ **与「被 cap 的完成」的边界**：被 cap 是机制逼停但**已经逼出最终答案**；超期是时间到了，
**没有答案**。
_Avoid_: 超时（那是某一只钟的读数，不是这个概念）、回合预算

**崩溃（crash）**：
进程被 kill（SIGKILL、断电），运行中的状态没来得及收口。**它是无序的**，与「中止」相对；恢复靠检查点：重启后从盘上续跑、不重复已完成的副作用（R4 的地盘）。
_Avoid_: 中止（那是 Ctrl+C 的有序停止）

**session（会话）**：
一次可恢复的工作单位。**盘上一个文件就是一条 session**（`<id>.jsonl`），外加一个旁挂的
`<id>.tools.jsonl` 工具日志——**所以删一条 session 要删两个文件**，这是 `checkpoint/saver.ts`
里那条长期没有主人的 ⚠️ 说的同一件事。由 LangGraph 的 `thread_id` 寻址：⚠️ **那是外来词，
它寻址的是 session，不是下面那条 thread**——值只在一处交接（`src/console/repl.ts` 交给
`configurable` 的那一行），但**名字不止一处**：`thread_id` 是配置键、`deleteThread` 是
`BaseCheckpointSaver` 的抽象方法，两个都改不动。所以规矩是**按位置**而不是按翻译：
**凡是装着 `thread_id` 的值的东西就叫 `threadId`**，而描述「文件里装的是什么」的散文说
session。`/clear` 开一条新 session，旧的仍然可寻址。

概念取自 pi（`packages/agent/docs/harness.md:86-99`），它把一条 session 定义成四个部分：
**entry 树**、**facts**（session 的名字、entry 的标签）、**lanes**（树上的命名游标）、
**用量账**。🔴 **我们今天只有第一个里的一条分支**，外加账。facts 没有（要列历史时，标题
只能拿第一条用户消息兜底——pi 自己也是这么兜的）；lanes 没有。
**账在 session 里，但不是一份独立的账本**：每条 ai 消息自带 `usage_metadata`，
派发的花费由 `Task` 的结果捎回来（子 agent 的消息不落盘），所以「这条 session 花了多少」
是从消息上加出来的。**只记 token，不记钱**——我们接多家 provider，价目表是别人的且会漂，
token 是能数准的那半（`dsh` 同样只计 token；pi 记了钱，因为它自带价目表）。
四个桶**互不重叠**：`uncachedInput / output / cacheRead / cacheWrite`
⚠️ **langchain 的约定相反**（`input_tokens` 含缓存、`cache_read` 是它的一个分解），
混着加就会把缓存算两次。**并且按模型分栏**：换了模型再 `--resume`，一个总数就是两家
provider 的 token 加在一起。这跟调研过的三个实现是同一条原则——**成本跟着产生它的那个工作单位走**
（pi 的 usage 行用 `entryId` 挂回 entry、Claude Code 挂在 assistant 消息上、
codex 一次 inference 一条记录并写明消耗与产出的 item）。⚠️ 两处不在账内：摘要那一跳的
消息进的是通道值不是消息列表，`elapsedMs` 根本不是 provider 的字段。⚠️ **有一样明确不抄**：pi 用 lane 让 subagent 共享同一段历史，我们反着
判过——`src/tools/task.ts` 里 `checkpointer: false`，**子 agent 的工作笔记不进用户的 session**。
⚠️ `/clear` 是开新 session，不是在同一条 session 上开分支（同 pi：`slash-commands.ts:37`
逐字 `{ name: "new", description: "Start a new session" }`，**分支在那边是另外两个动作**）。
_Avoid_: 线程、对话（指这个单位时）、thread（那是里面的一条）

**thread**：
一条 session 的 entry 树上的**一条分支**。在 LangGraph 里就是一条 checkpoint 血脉——每个
checkpoint 记着父指针（`langgraph/dist/pregel/loop.js:778`）——由 `checkpoint_id` 寻址。
🔴 **今天每条 session 恰好一条 thread。** 能有多条**不是还没做的功能，是还没有入口**：分支在
LangGraph 里是一等状态（`langgraph-checkpoint/dist/types.d.ts:19`，
`source: "input" | "loop" | "update" | "fork"`），我们的 saver 也早就存得下（`checkpoint/saver.ts`
的 `newestFirst()` 与 `asAppend()` 两处注释讲的都是 fork）。**缺的那样东西叫命名游标**：
LangGraph 没有 pi 的 lane，唯一的隐式游标是「id 最大的那个 checkpoint」，想寻址某一条分支
只能自己拿着 `checkpoint_id`。分支导航本身是 R10，判在范围外。
⚠️ **与 session 的边界**：session 是人能列、能选、能续、能删的那个单位（一个文件）；thread 是
模型看得见的那条线。**「这条 thread 至今的全部历史」是单次请求的分母，一条 session 的总和不是。**
_Avoid_: 会话、session（那是容器）

**种类（kind）**：
这程序能跑起来的一类 agent，**主 agent 也是一种**——不只是子 agent 的分类。一种由三样东西
定义：一个**身份（identity）**（一个字符串）、一份工具清单、一段提示词。身份不是标签，是**唯一
的名字**：账目、摘要账目（`${身份} summary`）、窗口事件、模型键入的 `subagent_type`、嵌套
graph 的名字，**全部从它派生**。每一种配的中间件由 `src/agents/kinds.ts` 的 `agentStack` 统一装配，
**顺序在那里被断言，不靠注释**。
_Avoid_: 角色、role、agent 类型（指这个概念时）

**秤（the scale）**：
`src/usage.ts` 的 `usageMeter`，一次 provider 请求记一条，**按种类的身份分栏**。
**每一项上下文工程改造都要用它称，判据挂在 `input` 与 `cached` 上**——`output` 抖，只作参考。

### 委派

**子 agent（subagent）**：
在一次工具调用内部跑起来的、嵌套的另一个 agent run。**它是机制层的词**——langchain 里没有这个
一等公民，只有「在工具体里再起一个 `createAgent`」这一种做法。它有自己的消息、自己的窗口、
自己的账。
_Avoid_: 子图、subgraph（那是 langgraph 的另一个东西）、协程

**Explore**：
我们注册的那一**种**（见「种类」）子 agent：**只读、单次、把结论交回**。名字取自 Claude Code
内置的同形态 agent 类型，不自造。**「只读」是它的工具清单决定的，不是一条检查**——它拿不到会
改动东西的工具。
_Avoid_: 探子、scout（2026-08-14 之前的内部叫法）、研究员、worker

**派遣（dispatch）**：
主 agent 通过 `Task` 工具起一个子 agent 的那一次动作。**一次派遣是无状态的**：子 agent 只看见
派遣时写下的目标，看不见这场对话。
_Avoid_: 调用、spawn、fork

**报告（report）**：
子 agent 交回来的那一条消息，**也是它唯一进入主 agent 上下文窗口的东西**。它的中间过程被丢弃，
所以**没写进报告的等于没有**。
_Avoid_: 返回值、结果、output

### 判据与证据

**判据（criterion）**：
一条把「做到没做到」变成二值的规则。**它要锚在行为上，不锚在实现上**——实现会变，
锚跟着就没了。
⚠️ **实例**：「动手之前先跑现成的检查」这条判据第一版锚在**工具名**上（第一次
`Write`/`Edit` 之前有没有跑检查）。可**工具选择本身就是会变的行为**：同一批题换一版
提示词之后，模型整个转去用 `Bash` 改文件（`build-initramfs-qemu` 的 `Write`+`Edit`
8 次 → 0 次），判据于是数不到任何东西，而那一栏读数看上去仍然像个数。
**改锚成「第一次改到文件」（含 `Bash` 里的重定向 / `sed -i` / `tee` / heredoc）之后
才重新可读。**
⚠️ **判据说错话的方向要说得出来。** 同一把尺子先后两次把「装依赖」判成「改文件」
（`apt-get install` 里的 `install`、`pandas>=2.0` 里的 `>=`），**两次都是同一个方向**：
把窗口提前关掉、读数偏小。**说得出方向的错是上界或下界，说不出方向的错是废数。**
⚠️ **分母只能是「有条件做这件事」的那些。** 判据本身对，用在一个它答不了的问题上，
一样是废数——「动手之前有没有先跑现成的检查」在 8 道 benchmark 题上数出 0，
而其中 **7 道的容器里根本没有可跑的检查**（评分器是评分阶段才拷进去的）。
那个 0 里绝大部分是**没得做**，不是**不做**，两者读起来一模一样。
_Avoid_: 指标、metric（那是连续量，判据是二值的）、标准

**观测面（observation surface）**：
判据实际读的那个东西——一条事件、一个字段、一段存档。
⚠️ **观测面自己要在真环境里验一次，测试绿不算。** `answer_cut` 从 2026-08-24 落地到
2026-08-28，**在生产里一次都没报过**，而它的测试全程是绿的：藏住它的是两层假绿——
探针量的是 `ChatOpenAI` 而不是我们的类，照文档造的 stub 又不是真实响应的形状。
**判据能不能亮，和判据的逻辑对不对，是两件要分别验的事。**
_Avoid_: 埋点、探针（探针是驱动的那一侧，不是被读的这一侧）

**证据（evidence）**：
判据印出来、让人能自己复核的那一小段原文。
⚠️ **印命中的那一段，不印开头。** 一条 shell 命令常有几十行，印开头等于把复核的人放在
一个截断的视野里——实测：尺子在一条命令上判「改了文件」，我看着它印的开头
（`ssh-keygen -A >/dev/null 2>&1…`）判它误报，而那条命令再往下真的有
`sed -i … /etc/ssh/sshd_config`。**尺子是对的，看开头的人是错的。**
_Avoid_: 日志、输出（证据是为复核印的，不是为观察印的）
